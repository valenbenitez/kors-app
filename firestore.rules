rules_version = '2';

// Deny all client access by default.
// Admin SDK bypasses rules; client SDK cannot read/write without Auth (future).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
